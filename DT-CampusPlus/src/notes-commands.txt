netstat -ano | findstr :8000

taskkill /PID 7732 /F

tasklist | findstr python

.\start_system.ps1


curl http://localhost:8000/current-status/canteen
git push -u origin feature/logging-and-structure
git config --get remote.origin.url
git branch -vv
git status
ls -R
